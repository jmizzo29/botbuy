# Egress IP source of truth

**Status: EMPTY.**

CTO has not published allowlisted egress IPs. UI rows stay Demo placeholders `X.X.X.X` with `— CTO provides egress IPs —`.

Do **not** invent dotted-decimal IPs, Vercel ranges, or Namecheap whitelist entries.

Namecheap production API remains Needs setup / `searchHttpReady=false` until this file lists real IPs **and** the registrar allowlist is updated.

Soft HOLD.
