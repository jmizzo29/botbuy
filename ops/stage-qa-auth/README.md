# Stage QA auth (local dumps)

**Soft HOLD.** Local QA credential dumps for staging Preview only.

`CREDENTIALS.json`, `.env.preview`, and other `.env*` files in this directory are **gitignored**. Do not commit them. This README is OK to track.

If you add a template, name it `.env.example` (empty values only). Never paste live keys into git, chat, or the data-room.
