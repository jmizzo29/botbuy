/**
 * The public URL is the pitch for everyone, including a signed-in phone.
 * Header shows My deals when a session exists. Do not bounce / to Sign in.
 */
export async function redirectSignedInFromLand() {
  return;
}
