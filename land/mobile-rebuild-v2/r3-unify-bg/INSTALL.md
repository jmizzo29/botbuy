# Land U1 — mesh-continuous (unify-bg)

**CEO LOCK.** READY-TO-SHIP → **`staging` only**. Soft-signal HOLD · land promote HOLD.

LEAN hotfix on the live **R3 lower-panel** skeleton. Not a rebuild. Soft HOLD full product.

Visual bar: phone fold `u1-mesh-continuous-phone-fold.png` (~390).

## Deltas vs R3

1. **Unify background** — A1 richer-mesh-deep FULL-BLEED (`position:absolute; inset:0`) behind the entire fold. Panel is a same-hue scrim ≤0.40, not solid `#050A0C`.
2. **Matched CTA pair** — Sign up teal fill + Sign in teal outline (`#2DD4BF` family). Equal metrics.
3. **About chrome bump** — 15px / 550 / `rgba(255,255,255,0.90)`.
4. **No teal page divider** — `border-top: none`. No sky↔copy hairline.
5. **No teal tip** under soft-spine on land chrome + hero mark.

## Out

- Solid `#050A0C` lower slab
- `border-top: 2px solid #2DD4BF`
- White/black ghost Sign in
- Dim tiny About (13px / 0.55)
- Install · Private beta · arc · one-liner · land footer
- Full product / connectors / vault

*BotBuyer Designer → CTO · U1 mesh-continuous READY-TO-SHIP → stage*
