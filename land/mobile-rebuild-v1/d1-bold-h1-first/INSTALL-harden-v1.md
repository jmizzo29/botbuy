# Land D1 — Quiet Capital harden v1

**John / CEO:** READY-TO-SHIP → **`staging` only**. Soft-signal HOLD.

Do **not** merge to `main` or ship production `botbuyer.ai` from this kit.

Parent lock: `INSTALL.md` (D1 bold-h1-first). Atmosphere **A1 richer-mesh-deep**. Chrome is soft-spine + BotBuyer. About-only. No Install.

Visual notes: `designer-d1-quiet-capital-harden-v1.md`.

## P1 (ship)

1. **Desktop optical system** — copy + CTA + rail are one packed band. Rail must not float as a right-edge island. H1-before-rail order stays. Align rail to the copy optical center / shared vertical band. Teal rule stays in the D1 stack.
2. **A1 mesh/noise** — low-opacity non-repeating mesh/noise on `.bb-atm-richer-mesh-deep::before` (`pointer-events: none`). Not a flat radial wash only.
3. **Desktop rail** — slightly stronger stroke / white connector opacity ~0.55. No glow. No new arc asset. No CSS invert.
4. **Phone chrome** — lockup −8–10% (~34px). About ~12px, clearer, hover/focus underline.
5. **Sign in ghost** — higher border + text contrast only. Same box metrics: equal flex, 48px, gap 10px.

## Keep

- Hierarchy: H1 → support → teal rule → arc rail → equal CTAs
- Phone center fold ~22rem · H1 first · arc under the hairline
- Soft-spine reverse lockup · About-only overlay · no Private beta · no Install
- Strings in `lib/brand.ts` — no copy string changes
- Ignore Vercel Live Feedback widget (not product)

## Out

- Orphan desktop rail island
- Flat A1 wash with no mesh/noise
- Glow / drop-shadow / CSS invert
- Install on land · Private beta in chrome · merge to `main`

*BotBuyer Designer → CTO · D1 Quiet Capital harden v1 → stage*
